{
  "name": "cinemagraphy",
  "version": "1.8.1",
  "packageManager": "pnpm@11.11.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env ./index.js",
    "dev-proxy": "node --env-file=.env ./proxyServer.js",
    "start": "node ./index.js",
    "start-proxy": "node ./proxyServer.js",
    "worker:dev": "wrangler dev",
    "worker:secrets": "node ./scripts/upload-worker-secrets.js",
    "worker:deploy": "wrangler deploy --secrets-file .dev.vars",
    "worker:check": "wrangler deploy --dry-run --secrets-file .dev.vars",
    "test": "node --test",
    "check": "node --test && node --check index.js && node --check proxyServer.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Stremio addon for streaming movies and series from Iranian providers",
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "cheerio": "^1.2.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "winston": "^3.19.0"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
